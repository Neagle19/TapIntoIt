<div class='breweries-wrapper'>
  <div class='breweries-title'>
    <h2>Breweries </h2>
  </div>

  <div class='breweries-container'>
    <div class='breweries-cards'>
      <% @breweries.each do |brewery| %>
        <%= link_to brewery_path(brewery) do %>
        <div class='brewery-card'>
          <div class='brewery-card-image' style="background-image: linear-gradient(-0deg, rgba(0,0,0,0.5) 0%, rgba(0,25,20,0.1) 50%), url(<%= cl_image_path(brewery.photo) %>">
            <div class='brewery-card-name'>
              <%= brewery.name %>
            </div>
          </div>
        </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>




<div class="wrapper" text-align="center">
  <div class='map-title'>
    <h2>Breweries Nearby </h2>
  </div>
  <div id="map" style="width: 100vw; height: 350px; margin-top: 90px;" data-markers="<%= @markers.to_json %>" data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
</div>


